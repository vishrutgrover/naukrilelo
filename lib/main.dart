import 'package:flutter/material.dart';
import 'package:naukrilelo/getinfo1.dart';
import 'package:naukrilelo/home.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    home: GetInfo1(), // Here I keep the page i am currently working just to launch it first.
    // In the final thing. We will have HomePage() here.
  ));
}
